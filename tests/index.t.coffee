assert = require 'assert'
describe 'getParameterByName', ->
  it 'should have a name', ->
    #location = 'http://localhost:8080?hangoutId=h1&userId=u1'
    #hangoutId = o.getParameterByName('hangoutId')
    #assert.equal(hangoutId, 'h1')
    #assert.equal(-1, [1,2,3].indexOf(5))
